class Main {
    function void main() {
        var String s;
        var Array v;

        let s = String.new(10);
        let v = Array.new(1);

        do s.setInt(-6725);
        let v[0] = s.intValue();
        
        return;
    }
}